# Configure these in Vercel project settings (Production environment).
# DO NOT commit a real .env.production file.

# Backend HTTPS URL (Caddy on the VPS)
NEXT_PUBLIC_API_BASE_URL=https://api.seudominio.com

# Tenant UUID (pilot tenant in production DB)
NEXT_PUBLIC_TENANT_ID=a7b8c9d0-1234-5678-9abc-def012345678
